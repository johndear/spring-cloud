<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="spring.cloud.dao.UserMapper">
 
  <select id="sumCurrentSubject" resultType="java.lang.Integer">
    	SELECT SUM(amount) FROM journalizing_info WHERE item_id=#{id} AND TYPE=#{type} AND data_source=#{datasource} and occurrence_time between #{startDate} and #{endDate} 
  </select>
  
  <select id="sumContainChildrenSubject" resultType="java.lang.Integer">
    	SELECT SUM(amount) FROM journalizing_info WHERE item_id like #{id}||'%' AND TYPE=#{type} AND data_source=#{datasource} and occurrence_time between #{startDate} and #{endDate} 
  </select>
 
</mapper>